const config = {
  environment: 11111111111,
  timeout: -1, // -1 for no timeout
  vaultName: 'your-vault-name-here',
  backupRole: 'nameOfBackupRole',
  inputType: 'LLL dd yyyy - HH:mm:ss',
  dynamoInputType: 'LLL-dd-yyyy-HH-mm-ss'
};

module.exports = config;
